/**
 * ...
 * @author Franco Ponticelli
 */

package rg.data;

import utest.Assert;
import rg.data.source.DataSourceArray;

class TestSources 
{
	public function testChain()
	{
		/*
		var data : Array<IDataSource<Dynamic>> = cast [
			ArrayDataSource.fromValues([1, 2, 3], "count", function(d, i) return { pos : i } ),
			ArrayDataSource.fromValues(['a', 'b', 'c'], "pos", function(d, i) return { char : d } )
		];
		*/
		//var sources = new ChartSources(data, "axis", "segment");
	/*	
		sources.onLoad.add(function(data) {
			trace(data);
		});
	*/
	}

	public function new() { }
}
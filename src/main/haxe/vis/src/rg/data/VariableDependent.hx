/**
 * ...
 * @author Franco Ponticelli
 */

package rg.data;

class VariableDependent<T> extends Variable<T, IAxis<T>> 
{
//	public var axis(default, null) : IAxis<T>;
/*
	public function new(type : String, axis : Null<IAxis<T>>, scaleDistribution : Null<ScaleDistribution>, ?min : T, ?max : T) 
	{
		super(type, scaleDistribution, min, max);
		this.axis = axis;
	}
*/

}